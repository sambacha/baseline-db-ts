'use strict';


/**
 * /metadata/contractAddress -> gets the contractAddress
 * inserts a leaf object
 *
 * contentType String 
 * no response value expected for this operation
 **/
exports./metadata/contractAddress>getsthecontractAddress = function(contentType) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/contractAddress -> inserts the contract address of the tree
 * inserts a node object
 *
 * no response value expected for this operation
 **/
exports./metadata/contractAddress>insertsthecontractaddressofthetree = function() {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/contractInterface -> gets the contractInterface
 * inserts a leaf object
 *
 * contentType String 
 * no response value expected for this operation
 **/
exports./metadata/contractInterface>getsthecontractInterface = function(contentType) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/contractInterface -> inserts the contract address of the tree
 * inserts a node object
 *
 * body String 
 * no response value expected for this operation
 **/
exports./metadata/contractInterface>insertsthecontractaddressofthetree = function(body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/latestLeaf -> get the latestLeaf metadata object
 * inserts a leaf object
 *
 * contentType String 
 * no response value expected for this operation
 **/
exports./metadata/latestLeaf>getthelatestLeafmetadataobject = function(contentType) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/latestLeaf -> updates the latestLeaf metadata object
 * inserts a leaf object
 *
 * no response value expected for this operation
 **/
exports./metadata/latestLeaf>updatesthelatestLeafmetadataobject = function() {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/latestRecalculation -> gets the latestRecalculation metadata object
 * inserts a leaf object
 *
 * contentType String 
 * no response value expected for this operation
 **/
exports./metadata/latestRecalculation>getsthelatestRecalculationmetadataobject = function(contentType) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/latestRecalculation -> updates the latestRecalculation metadata object
 * inserts a leaf object
 *
 * no response value expected for this operation
 **/
exports./metadata/latestRecalculation>updatesthelatestRecalculationmetadataobject = function() {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata/treeHeight -> establishes the treeHeight for a db
 * inserts a node object
 *
 * no response value expected for this operation
 **/
exports./metadata/treeHeight>establishesthetreeHeightforadb = function() {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * /metadata -> gets the tree's metadata object
 *
 * contentType String 
 * no response value expected for this operation
 **/
exports./metadata>getsthetree'smetadataobject = function(contentType) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

